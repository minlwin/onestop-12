import Page from "../../../ui/page";

export default function LedgerEntryEdit() {
    return (
        <Page title={`Ledger Entry Edit`} icon={<i className="bi-pencil"></i>}>
            
        </Page>
    )
}